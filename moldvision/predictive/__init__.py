"""Predictive model tooling — startup suggestion models.

This sub-package extends MoldVision from a CV-only model factory to a
general ARIA model factory.  It provides:

- ``training_row_loader``: load, validate, filter and extract feature matrices
  from ``training_row_v1`` JSONL files produced by MoldTrace.
"""
